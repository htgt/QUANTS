nextflow_pipeline {

    name "Test Workflow main.nf"
    script "main.nf"

    test("Should run paired-end FLASH2 merging, QC, trimming, and pyquest quantification without failures") {

        def resolvedSheet = ManifestResolver.resolveSheet(projectDir, "raw-fastq-pe.csv")

        when {
            params {
                input = "${resolvedSheet}"
                single_end = false
                input_type = "fastq"
                raw_sequencing_qc = true
                adapter_trimming = "cutadapt"
                adapter_trimming_qc = true
                primer_trimming = "cutadapt"
                primer_trimming_qc = true
                read_merging = "flash2"
                read_merging_qc = true
                quantification = "pyquest"
                pyquest_library_converter_options = "-N 1 -S 24"
            }
        }

        then {
            assert workflow.success
        }

    }

}
